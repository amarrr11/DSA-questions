/*Given 2 strings divide eachh*/

#include<bits/stdc++.h>
using namespace std;

bool solve(string &s1,string &s2,int i,int n){

    if(s1.substr(i,n-i)==s2.substr(i+1)) return true;
    if(s1.substr(i,n-1)==s2.substr(n-i)) return true;
    if(s1.substr())

    for(int k=i;k<=n-1;k++){
        
        else if
        return solve(s1,s2,i,n-1) or solve(s1,s2,k+1,n-1);
    }
}

int main(){
    string 
}