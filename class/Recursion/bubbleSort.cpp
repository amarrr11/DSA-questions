#include<bits/stdc++.h>
using namespace std;

void sort(vector<int> &v,int n){
    
}

int main(){
    vector<int>v;
    v.push_back(2);
    v.push_back(8);
    v.push_back(1);
    v.push_back(12);
    v.push_back(20);

    sort(arr,arr.size());
}